function show(...args) {
    // Q: Print number of arguments and the first one.
    console.log(`Number of Argus: ${args.length} and First 0-index: ${args[0]}`)
  }
  show("A", "B", "C"); // Output: Number of Argus: 3 and First 0-index: A
  